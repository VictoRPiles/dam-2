<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
            <record model="ir.ui.view" id="controller_depends.list">
              <field name="name">controller_depends list</field>
              <field name="model">controller_depends.controller_depends</field>
              <field name="arch" type="xml">
                <tree>
                  <field name="name"/>
                  <field name="value"/>
                  <field name="value2"/>
                </tree>
              </field>
            </record>
        -->

        <!-- actions opening views on models -->
        <!--
            <record model="ir.actions.act_window" id="controller_depends.action_window">
              <field name="name">controller_depends window</field>
              <field name="res_model">controller_depends.controller_depends</field>
              <field name="view_mode">tree,form</field>
            </record>
        -->

        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="controller_depends.action_server">
              <field name="name">controller_depends server</field>
              <field name="model_id" ref="model_controller_depends_controller_depends"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "tree,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->
        <!--
            <menuitem name="controller_depends" id="controller_depends.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="controller_depends.menu_1" parent="controller_depends.menu_root"/>
            <menuitem name="Menu 2" id="controller_depends.menu_2" parent="controller_depends.menu_root"/>
        -->
        <!-- actions -->
        <!--
            <menuitem name="List" id="controller_depends.menu_1_list" parent="controller_depends.menu_1"
                      action="controller_depends.action_window"/>
            <menuitem name="Server to list" id="controller_depends" parent="controller_depends.menu_2"
                      action="controller_depends.action_server"/>
        -->
    </data>
</odoo>